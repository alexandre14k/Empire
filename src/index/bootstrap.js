// small bootstrap for index to mirror folder-per-file structure
(function () {
  if (!window.Empire) window.Empire = {};
  // previously src/index.js bootstraps the game; we keep that file as-is but
  // provide a folder with helpers for future refactor.
})();
