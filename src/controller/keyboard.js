// small keyboard helper placeholder
(function () {
  if (!window.Empire) window.Empire = {};
  Empire.controller = Empire.controller || {};

  Empire.controller.keyboard = {
    // future helpers for keyboard input
  };
})();
